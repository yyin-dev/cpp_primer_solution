#include <iostream>
#include <string>
#include <vector>
using std::cout;
using std::endl;
using std::string;
using std::vector;

struct matrix {};
bool compare(matrix &m1, matrix &m2);
vector<int> change_val(int i, vector<int>::iterator iterator);
void test(double, int);  // You can igore parameter names. But it's good to have it.

int main() {
    return 0;
}
